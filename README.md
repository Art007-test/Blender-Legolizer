# Blender-Legolizer
A blender addon that use geometry node to transform a mesh in lego

the addon comes with a shader made for lego brick


there is also an explosion transformed into lego brick
this explosion use an other geometry node setup that absolutly can't run in real time, it is more a proof of concept
